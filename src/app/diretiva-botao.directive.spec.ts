import { DiretivaBotaoDirective } from './diretiva-botao.directive';

describe('DiretivaBotaoDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaBotaoDirective();
    expect(directive).toBeTruthy();
  });
});
