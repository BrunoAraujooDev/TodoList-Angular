import { DiretivaInputDirective } from './diretiva-input.directive';

describe('DiretivaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaInputDirective();
    expect(directive).toBeTruthy();
  });
});
